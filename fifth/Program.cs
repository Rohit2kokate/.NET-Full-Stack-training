using System;

namespace fifth_Project{
    class Program{
        static void Main(string[] args){
           /* Console.WriteLine("Welcome to the fifth project!");

            Console.WriteLine("Enter the Element of Array");
            int n=int.Parse(Console.ReadLine());
            int[] arr=new int[n];
            System.Console.WriteLine("Enter the array element");
            for (int i = 0; i < n; i++)
            {
                arr[i]=int.Parse(Console.ReadLine());
            }
            System.Console.WriteLine("Array elemnt are");
            for (int i = n-1; i >= 0; i--)
            {
                Console.Write(arr[i]);
            }*/
            Sum.sum();
            
        }
    }
}